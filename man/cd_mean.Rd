% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R, R/criteria_old.R
\name{cd_mean}
\alias{cd_mean}
\title{Coefficient of Determination (R²) based on experimental design literature}
\usage{
cd_mean(train, test = NULL, P, lambda = 1e-06, C = NULL, normalized = FALSE)

cd_mean(train, test = NULL, P, lambda = 1e-06, C = NULL, normalized = FALSE)
}
\arguments{
\item{train}{Vector of training set individual names}

\item{test}{Vector of test set individual names (if NULL, uses training set)}

\item{P}{Prediction matrix with individuals as rows and variables as columns}

\item{lambda}{Ridge regularization parameter for numerical stability (default: 1e-6)}

\item{C}{Contrast matrix for specific linear combinations (default: NULL)}

\item{normalized}{Whether to normalize by trace for scale-invariance (default: FALSE)}
}
\value{
Mean R² value (proportion of variance explained)

Mean R² value (proportion of variance explained)
}
\description{
Computes the coefficient of determination as the proportion of variance explained
by the model, using the hat matrix diagonal (leverage values).

Computes the coefficient of determination as the proportion of variance explained
by the model, using the hat matrix diagonal (leverage values).
}
\details{
Computes R² as the average leverage from the hat matrix, which represents
the proportion of variance explained by the model in experimental design.

Formula: R² = tr(H)/n where H = X(X'X)⁻¹X' is the hat matrix

This is the standard definition used when response data is not available.

Computes R² as the average leverage from the hat matrix, which represents
the proportion of variance explained by the model in experimental design.

Formula: R² = tr(H)/n where H = X(X'X)⁻¹X' is the hat matrix

This is the standard definition used when response data is not available.
}
\examples{
# Create example data with some outliers
set.seed(789)
P <- matrix(rnorm(200), 40, 5)
# Add some outliers
P[c(5, 15, 25), ] <- P[c(5, 15, 25), ] + 3
rownames(P) <- paste0(\"ind\", 1:40)

train <- paste0(\"ind\", 1:20)
test <- paste0(\"ind\", 21:35)

# Compute Coefficient of Determination (R²)
r2_mean_val <- cd_mean(train, test, P)
r2_norm_val <- cd_mean(train, test, P, normalized = TRUE)

print(paste(\"Mean R² (proportion of variance explained):\", round(r2_mean_val, 4)))
print(paste(\"Normalized R²:\", round(r2_norm_val, 4)))

# Compare with PEV
pev_val <- pev_mean(train, test, P)
print(paste(\"PEV for comparison:\", round(pev_val, 6)))

}
\references{
Fedorov, V.V. (1972). Theory of Optimal Experiments. Academic Press.
Atkinson, A.C., Donev, A.N., Tobias, R.D. (2007). Optimum Experimental Designs.

Fedorov, V.V. (1972). Theory of Optimal Experiments. Academic Press.
Atkinson, A.C., Donev, A.N., Tobias, R.D. (2007). Optimum Experimental Designs.
}
