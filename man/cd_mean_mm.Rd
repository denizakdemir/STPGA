% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R, R/criteria_old.R
\name{cd_mean_mm}
\alias{cd_mean_mm}
\title{Coefficient of Determination (R²) for Mixed Models}
\usage{
cd_mean_mm(train, test = NULL, P, K, lambda = 1e-06, C = NULL, Vg = 1, Ve = 1)

cd_mean_mm(train, test = NULL, P, K, lambda = 1e-06, C = NULL, Vg = 1, Ve = 1)
}
\arguments{
\item{train}{Vector of training set individual names}

\item{test}{Vector of test set individual names (if NULL, uses training set)}

\item{P}{Prediction matrix with individuals as rows and markers as columns}

\item{K}{Kinship/relationship matrix (symmetric, positive definite)}

\item{lambda}{Ridge regularization parameter for numerical stability (default: 1e-6)}

\item{C}{Contrast matrix for specific linear combinations (default: NULL)}

\item{Vg}{Genetic variance component (default: 1)}

\item{Ve}{Error variance component (default: 1)}
}
\value{
Mean R² value for mixed models (higher indicates better prediction accuracy)

Mean CD value for mixed models (higher indicates better prediction accuracy)
}
\description{
Computes the coefficient of determination for mixed models, measuring
the expected prediction accuracy of random effects (breeding values).

Computes the mean Coefficient of Determination for mixed models, measuring
the expected prediction accuracy of random effects (breeding values).
}
\details{
The Coefficient of Determination for BLUP of random effects is defined as:
R² = 1 - PEV/σ²ᵤ

This represents the proportion of genetic variance that can be predicted,
or the reliability of breeding value prediction.

The Coefficient of Determination for BLUP of random effects is defined as:
CD = 1 - PEV/σ²ᵤ
where PEV is the prediction error variance and σ²ᵤ is the genetic variance.

This can also be expressed in terms of heritability:
CD = reliability of breeding value prediction

For genomic selection, CD represents the expected accuracy squared of 
genomic estimated breeding values (GEBVs).
}
\examples{
# Create example data
set.seed(202)
n <- 25
P <- matrix(rnorm(n * 40), n, 40)
rownames(P) <- paste0("ind", 1:n)

# Kinship matrix with some strong relationships
K <- matrix(0.05, n, n)
diag(K) <- 1
# Add some close relationships
K[1:5, 1:5] <- 0.7
diag(K[1:5, 1:5]) <- 1
rownames(K) <- colnames(K) <- rownames(P)

train <- paste0("ind", 1:15)
test <- paste0("ind", 16:20)

# Compute mixed model Coefficient of Determination
cd_mm <- cd_mean_mm(train, test, P, K)
print(paste("Mixed model CD:", round(cd_mm, 6)))

# Compare with leverage-based CD
cd_regular <- cd_mean(train, test, P)
print(paste("Leverage-based CD:", round(cd_regular, 6)))

}
\references{
Henderson, C.R. (1984). Applications of Linear Models in Animal Breeding.
}
