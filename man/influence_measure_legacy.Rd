% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R, R/criteria_old.R
\name{influence_measure_legacy}
\alias{influence_measure_legacy}
\title{Legacy influence measure (for backward compatibility)}
\usage{
influence_measure_legacy(
  train,
  test = NULL,
  P,
  lambda = 1e-06,
  C = NULL,
  normalized = FALSE
)

influence_measure_legacy(
  train,
  test = NULL,
  P,
  lambda = 1e-06,
  C = NULL,
  normalized = FALSE
)
}
\arguments{
\item{train}{Vector of training set individual names}

\item{test}{Vector of test set individual names (if NULL, uses training set)}

\item{P}{Prediction matrix with individuals as rows and variables as columns}

\item{lambda}{Ridge regularization parameter (default: 1e-6)}

\item{C}{Contrast matrix (default: NULL)}

\item{normalized}{Whether to normalize (default: FALSE)}
}
\value{
Mean influence measure value

Mean influence measure value
}
\description{
Computes the leverage-based influence measure that was incorrectly labeled 
as "coefficient of determination" in the original implementation.

Computes the leverage-based influence measure that was incorrectly labeled 
as "coefficient of determination" in the original implementation.
}
\details{
This function computes what the original cd_mean actually calculated:
Influence = (leverage_i / (1 - leverage_i)²) × p

This is related to Cook's distance without the residual component.
For proper R², use cd_mean() which computes average leverage.

This function computes what the original cd_mean actually calculated:
Influence = (leverage_i / (1 - leverage_i)²) × p

This is related to Cook's distance without the residual component.
For proper R², use cd_mean() which computes average leverage.
}
